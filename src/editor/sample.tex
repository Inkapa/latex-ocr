% Welcome to LaTeX OCR.
% Edit the code on the left; the preview on the right updates automatically.

Here is inline math: $x^2 + y^2 = z^2$.

\begin{equation}
    E = mc^2
\end{equation}

And a small aligned derivation:

\begin{align}
    (a + b)^2 &= a^2 + 2ab + b^2 \\
    \int_0^1 x^2\,dx &= \frac{1}{3}
\end{align}

A fraction, a root and a limit:

\[
    \lim_{n \to \infty} \sqrt[n]{\frac{1}{n!}} = 0
\]

% Tip: drag a rectangle over any math on screen and the "Snip & OCR"
% button turns it back into LaTeX for you.
